name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: k88hudson/setup-plz@v1
      - run: corepack enable
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - uses: astral-sh/setup-uv@v7
      - uses: ./.github/actions/setup-rust
      - run: pnpm install
      - run: plz docs build
      - run: plz models build
        env:
          BASE_URL: /cfa-simulator/
      - name: Assemble site
        run: |
          mkdir -p _site
          cp -r models/dist/* _site/
          cp -r docs/.vitepress/dist _site/docs
          cp _site/index.html _site/404.html
      - uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
