name: Publish Package

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - uses: actions/checkout@v6
      - uses: k88hudson/setup-plz@v1
      - run: corepack enable
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm
      # Pin to npm 11.x: npm 12.0.0 ships a broken provenance path
      # ("Cannot find module 'sigstore'"). Drop the pin once 12.x is fixed.
      - run: npm install -g npm@11
      - run: pnpm install
      - run: plz build
      - run: pnpm publish -r --access public --no-git-checks
        env:
          NPM_CONFIG_PROVENANCE: true
          NODE_AUTH_TOKEN: ""
